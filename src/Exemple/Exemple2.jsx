const addition = (a, b) => a + b;
const x = "toto";

const MonComposant = () => {
    return (
        <>
            <h1>{console.log(addition(2,4))}</h1>
        </>
    );
}

export default MonComposant;